# orchestrator/ · 编排决策引擎

> 运行时由 loop-check closure 模式自动维护。不存原始数据，只存最优决策结论。
> 树形加载：Agent 先读 `_index.md` → 定位分支 → 只加载目标叶子文件。
>
> ⚠️ orchestrator/ 是最后初始化的数据文件——需等待 task/logs 积累 ≥3 次同类任务记录、skills/ 评分就绪后，才会写入第一个最优配置。在此之前该目录只有 `_index.md` 空壳。
>
> 本文件是格式参考——实际目录在 Skill 首次加载时自动初始化。
>
> 数据来源：task/logs/（A/B 对比）。读取方：主 Agent 编排时按需查询；反思时读取，汇入 think.md。
>
> ⚠️ 以下路径为相对于 `{SOFAGENT_DATA}/` 的简写。实际读写时请加 `{SOFAGENT_DATA}/` 前缀。

---

<!-- 
  目录结构（大类→子类→具体任务，缩小搜索空间）：

  orchestrator/
  ├── _index.md                      ← 决策树总目录（≤50 行）
  ├── 研发/
  │   ├── _index.md
  │   ├── 代码生成/
  │   │   ├── 前端组件.md           ← 该任务的最优配置
  │   │   └── 后端API.md
  │   └── ...
  ├── 运营/
  │   └── 数据分析/
  │       ├── 简单报表.md
  │       └── 复杂建模.md
  └── 市场/
      └── ...

  每个叶子文件格式（10-15 行），存该任务类型的当前最优配置：

  # 前端组件.md
  ## 前端组件 | 使用 7 次 | 胜率 85%
  **最优拆法**：子任务1→子任务2
  **最优 Skill**：Skill A + Skill B
  **最优模型**：Pro（子任务1）+ Flash（子任务2）
  **最优角色**：Agent模板A · Agent模板B
  **最近更新**：2026-06-09（本次优于历史最优，已覆盖）
  **首次预估步数**：80（首次执行时的步数预估，跑满 3 次后自动校准）
  **中间检查阈值**：步数上限 120 | token 上限 18,000 | 重试上限 3
  **worktree**：是（子任务1 和 子任务2 共享同一仓库）

  主 Agent 编排前先查 orchestrator/ → 有记录直接用 → 没记录走完整编排。
  活跃区控制在 ~3,000 token 以内。准入：最近 7 天内引用过 OR 使用 ≥5 次且胜率 >80%。连续 30 天未命中 OR 胜率 <50% → 归档。归档后跑满 3 次恢复激活。
  
  不需要手动写任何东西。Agent 会自动维护。
-->
